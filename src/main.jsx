import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

**File 4: `src/App.jsx`** — this is the key one
```
1. Go to your repo
2. Create new file: src/App.jsx
3. Copy ALL the contents of openclaw-terminal.jsx into it
4. Commit
```

---

## After adding those files
```
1. Go back to CodeSandbox
2. Open your sandbox
3. In the left panel — click the refresh icon
   or re-import from GitHub
4. CodeSandbox will detect package.json
   and install dependencies automatically
5. The terminal should show: "ready in Xms"
6. Preview panel on the right shows the dashboard
```

---

## Shareable live link

Once it's running, CodeSandbox gives you a URL like:
```
https://strange-wave.csb.app
